apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cloudkeeper.fullname" . }}-test-connection"
  labels:
    {{- include "cloudkeeper.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cloudkeeper.fullname" . }}-ckcore:{{ .Values.ckcore.service.port }}']
  restartPolicy: Never
