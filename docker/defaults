# The following are default values.
# They can be overridden by supplying the -e VAR=value option to the containerizer.

# The timezone this container is running in
TZ=${TZ:-Etc/UTC}

# User and group id for the user running valheim-server
PUID=${PUID:-0}
PGID=${PGID:-0}

# Pre-shared key to use between components
PSK=${PSK-changeme}

# Supervisor http
SUPERVISOR_HTTP=${SUPERVISOR_HTTP:-false}
SUPERVISOR_HTTP_PORT=${SUPERVISOR_HTTP_PORT:-9001}
SUPERVISOR_HTTP_USER=${SUPERVISOR_HTTP_USER:-admin}
SUPERVISOR_HTTP_PASS=${SUPERVISOR_HTTP_PASS:-}

# Syslog settings
SYSLOG_REMOTE_HOST=${SYSLOG_REMOTE_HOST:-}
SYSLOG_REMOTE_PORT=${SYSLOG_REMOTE_PORT:-514}
SYSLOG_REMOTE_AND_LOCAL=${SYSLOG_REMOTE_AND_LOCAL:-true}

# Legacy Cloudkeeper startup
LEGACY_STARTUP=${LEGACY_STARTUP:-false}

# Which services to start
START_STACK=${START_STACK:-true}
START_DNSMASQ=${START_DNSMASQ:-$START_STACK}
START_GRAPHDB=${START_GRAPHDB:-$START_STACK}
START_TSDB=${START_TSDB:-$START_STACK}
START_RESOTOCORE=${START_RESOTOCORE:-$START_STACK}
START_RESOTOWORKER=${START_RESOTOWORKER:-$START_STACK}
START_RESOTOMETRICS=${START_RESOTOMETRICS:-$START_STACK}

# GraphDB settings
GRAPHDB_ROOT_PASSWORD=${GRAPHDB_ROOT_PASSWORD:-changeme}
GRAPHDB_SERVER_ENDPOINT=${GRAPHDB_SERVER_ENDPOINT:-tcp://127.0.0.1:8529}
GRAPHDB_DATABASE_DIRECTORY=${GRAPHDB_DATABASE_DIRECTORY:-/data/db}

# TSDB settings
TSDB_DATABASE_DIRECTORY=${TSDB_DATABASE_DIRECTORY:-/data/tsdb}
TSDB_CONFIG_FILE=${TSDB_CONFIG_FILE:-/usr/local/tsdb/prometheus.yml}
TSDB_RETENTION_TIME=${TSDB_RETENTION_TIME:-730d}

# resotocore settings
RESOTOCORE_GRAPHDB_SERVER=${RESOTOCORE_GRAPHDB_SERVER:-http://127.0.0.1:8529}
RESOTOCORE_GRAPHDB_DATABASE=${RESOTOCORE_GRAPHDB_DATABASE:-resoto}
RESOTOCORE_GRAPHDB_USERNAME=${RESOTOCORE_GRAPHDB_USERNAME:-resoto}
RESOTOCORE_GRAPHDB_PASSWORD=${RESOTOCORE_GRAPHDB_PASSWORD:-changeme}
RESOTOCORE_HOST=${RESOTOCORE_HOST:-0.0.0.0}

RESOTOCORE_URI=${RESOTOCORE_URI:-http://127.0.0.1:8900}
RESOTOCORE_WS_URI=${RESOTOCORE_WS_URI:-ws://127.0.0.1:8900}
RESOTOCORE_PSK=${RESOTOCORE_PSK-$PSK}

# resotoworker settings
RESOTOWORKER_RESOTOCORE_URI=${RESOTOWORKER_RESOTOCORE_URI:-$RESOTOCORE_URI}
RESOTOWORKER_RESOTOCORE_WS_URI=${RESOTOWORKER_RESOTOCORE_WS_URI:-$RESOTOCORE_WS_URI}
RESOTOWORKER_PSK=${RESOTOWORKER_PSK-$PSK}

# resotometrics settings
RESOTOMETRICS_RESOTOCORE_URI=${RESOTOMETRICS_RESOTOCORE_URI:-$RESOTOCORE_URI}
RESOTOMETRICS_RESOTOCORE_WS_URI=${RESOTOMETRICS_RESOTOCORE_WS_URI:-$RESOTOCORE_WS_URI}
RESOTOMETRICS_PSK=${RESOTOMETRICS_PSK-$PSK}

# resotosh settings
RESOTOSHELL_PSK=${RESOTOSHELL_PSK-$PSK}
