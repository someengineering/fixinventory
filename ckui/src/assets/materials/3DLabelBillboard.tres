[gd_resource type="ShaderMaterial" load_steps=2 format=2]

[sub_resource type="Shader" id=8]
code = "shader_type spatial;
render_mode unshaded;

uniform sampler2D texture_albedo : hint_albedo;
uniform float camera_offset = 1.0;
uniform float camera_offset_min_dist = 50.0;
uniform float label_base_distance = 0.01;
uniform float max_depth = 6000.0;
uniform float fadeout_dist = 1000000.0;

float ease(float x) {
	return 1.0 - (1.0-x) * (1.0-x);
}

void vertex() {
	float PI = 3.1415926;
	vec3 ro = (CAMERA_MATRIX * vec4(0.0, 0.0, 0.0, 1.0)).xyz;// Get camera position in World space coordinates
	vec3 p = ((WORLD_MATRIX)*vec4(VERTEX,1.0)).xyz;// Get fragment position in world space coordinates
	float depth = abs( clamp(  (length(p-ro) + camera_offset_min_dist ) / max_depth, 0.0, 1.0) - 1.0 );
	depth = ease(depth);
	
	MODELVIEW_MATRIX = INV_CAMERA_MATRIX * mat4(CAMERA_MATRIX[0],CAMERA_MATRIX[1],CAMERA_MATRIX[2],WORLD_MATRIX[3]);
	MODELVIEW_MATRIX = MODELVIEW_MATRIX * mat4(vec4(length(WORLD_MATRIX[0].xyz), 0.0, 0.0, 0.0),vec4(0.0, length(WORLD_MATRIX[1].xyz), 0.0, 0.0),vec4(0.0, 0.0, length(WORLD_MATRIX[2].xyz), 0.0),vec4(0.0, 0.0, 0.0, 1.0));
	VERTEX += camera_offset * (depth + label_base_distance) * ((MODELVIEW_MATRIX * vec4(0.0, 0.0, 1.0, 0.0)).xyz);
}


void fragment() {
	float PI = 3.1415926;
	vec3 ro = (CAMERA_MATRIX * vec4(0.0, 0.0, 0.0, 1.0)).xyz;// Get camera position in World space coordinates
	vec3 p = ((WORLD_MATRIX)*vec4(VERTEX,1.0)).xyz;// Get fragment position in world space coordinates
	float depth = abs( clamp( max( length(p-ro)-fadeout_dist, 0.0) / fadeout_dist, 0.0, 1.0) - 1.0 );
	depth = ease(depth);
	
	vec4 albedo_tex = texture(texture_albedo,UV);
	ALBEDO = albedo_tex.rgb;
	METALLIC = 0.;
	ROUGHNESS = 1.;
	SPECULAR = 0.;
	ALPHA = albedo_tex.a * depth;
}
"

[resource]
resource_local_to_scene = true
shader = SubResource( 8 )
shader_param/camera_offset = 0.25
shader_param/camera_offset_min_dist = 2500.0
shader_param/label_base_distance = 0.7
shader_param/max_depth = 5000.0
shader_param/fadeout_dist = 600000.0
